# 프로젝트 기록 — 보험 뉴스 다이제스트 → Genie's Insurance Note

이 문서는 **처음부터 지금까지 무엇을 만들었고, 왜 그렇게 결정했고, 무엇이 틀렸는지**를
남긴 기록입니다. 사용법은 [README.md](README.md), 여기는 배경과 판단 근거입니다.

- 리포: `https://github.com/yhyoo311-ai/insurance-news` (public, `main`)
- 로컬: `C:\Users\Wonjae Yoo\Desktop\개발\Isurnace news`
- 최종 상태: 웹 배포 완료(`https://insuranceinfo.pages.dev`, 비밀번호 게이트) · 데이터 기준일 2025-12-31
- 데이터: 35개사, 개략치 0 — 재무는 DART 24 + FISIS 11, K-ICS 는 35개사 전부 FISIS 실측

---

## 1. 전체 그림

```
                         ┌─────────────────────────────────────────┐
                         │  GitHub 리포 = 데이터베이스              │
                         │   settings.json     (뉴스 다이제스트 설정) │
                         │   data/insurers.json (보험사 35개사 마스터)│
                         └───────┬──────────────────┬──────────────┘
                                 │ 읽기              │ 읽기·쓰기
              ┌──────────────────┴────┐        ┌────┴──────────────────┐
              │ daily.yml  매일 07:00 │        │ 웹 관리자 (브라우저)   │
              │ 뉴스 수집→요약→텔레그램│        │ GitHub API 로 직접 커밋│
              └───────────────────────┘        └───────────────────────┘
                                 │
              ┌──────────────────┴────────────────────┐
              │ site.yml  평일 09~16:30 30분마다       │
              │ Yahoo·네이버 조회 → dist/ → Cloudflare │
              └──────────────────┬────────────────────┘
                                 ▼
                     Cloudflare Pages + _worker.js
                     (비밀번호 게이트, 무료 — 5.1 참고)
```

**핵심 설계 판단: GitHub 를 그대로 데이터베이스로 썼다.**
무료 호스팅은 파일시스템이 휘발성이라 설정을 저장할 곳이 없다. 그런데 이 프로젝트는
이미 다이제스트가 GitHub 의 `settings.json` 을 읽고 있었다. 그래서 서버·DB 를 새로
두지 않고, 웹 관리자가 GitHub Contents API 로 커밋하게 만들었다. 기존 파이프라인이
한 줄도 바뀌지 않고 그대로 돈다.

---

## 2. 1기 — 보험 뉴스 텔레그램 다이제스트 (2026-07-28, 이번 작업 전)

커밋 7개로 완성된 기존 시스템. 이번 작업에서 **파이프라인 로직은 건드리지 않았다.**

| 커밋 | 내용 |
|------|------|
| `b84d40b` | 초기 구축 — 네이버 수집 + Gemini 요약 + 텔레그램 발송 |
| `1715c80` | 노이즈 필터(채용·ETF·건보료) + 롯데손해보험 핀 지정 |
| `2a218ff` | 발송 대상 다중화 (개인 채팅 + 공개 채널) |
| `4cc7b06` | 제도·규제·킥스·GA 비중 강화 |
| `748be51` | 해외진출 주제 반영 |
| `800f40d` | 설정 대시보드 — `settings.json` 분리 + Flask 웹 UI |
| `8d74346` | 대구분 섹션(6개)별 3~5건 정리 구조로 개편 |

```
수집(네이버API·RSS) → 중복제거 → 섹션 배정 → 섹션별 상위 선별 → Gemini 요약 → 텔레그램
```

`main.py` · `src/{collect,filters,classify,rank,summarize,notify}.py` · `config.py` ·
`.github/workflows/daily.yml` — **모두 그대로 유지.**

---

## 3. 2기 — 보험사 정보 시스템 (히트맵 + 회사 패널)

### 요구사항

> 지금 만든 대시보드 기능을 하나의 기능(지금 있는 기능은 관리자모드 안에 뉴스 텔레그램
> 설정으로)으로 해서 보험사별 정보를 볼 수 있는 정보웹사이트를 만들고 싶어
> 1. 국내 보험사를 시가총액 혹은 기업규모(비상장사용 추정규모)로 히트맵 표시
> 2. 해당 보험사 클릭시 주가차트, 주주, 재무분석, 최근 관련 뉴스 3개

참고 이미지로 중국 기업 지형도(트리맵 + 우측 슬라이드 패널)를 받았다.

### 확인한 선택

| 항목 | 선택 | 근거 |
|------|------|------|
| 히트맵 크기 기준 | **토글 (총자산 / 시가총액)** | 총자산은 상장·비상장 37개사를 같은 기준으로 비교. 시총은 상장 12개사만 |
| 대상 범위 | **생보+손보 전체 37개사** | `config.py` 의 회사 사전을 그대로 활용 |
| 실행 방식 | 로컬 우선, 웹 업로드도 검토 | → 4기에서 웹 배포로 확장 |

### 아키텍처

기존 Flask(`dashboard.py`)를 확장하지 않고 **`webapp.py` 로 재구성**했다.
인라인 f-string HTML 로는 트리맵을 유지보수할 수 없어 `templates/` + `static/` 로 분리.

```
webapp.py               /  (히트맵) · /api/* · /admin/news · /admin/companies
templates/_site.html    히트맵 본문 — 로컬 Flask 와 정적 스냅샷이 공용
static/js/app.js        squarified treemap · 회사 패널 · SVG 차트 · 표 보기
static/css/app.css      팔레트 · 레이아웃 · 라이트/다크
data/insurers.json      보험사 마스터
dashboard.py            기존 진입점 → webapp 관리자모드로 리다이렉트 (습관 보존)
```

### 색 설계 — 계산으로 검증

등락률은 **diverging**(양극성)이므로 파랑(하락) ↔ 중립 회색(보합) ↔ 빨강(상승).
눈대중 대신 검증 스크립트로 명도 단조성·인접 ΔL·대비를 확인해 **양쪽 모드 전부 PASS**.

| | 하락 3 / 2 / 1 | 보합 | 상승 1 / 2 / 3 |
|---|---|---|---|
| 라이트 | `#184f95` `#2a78d6` `#86b6ef` | `#f0efec` | `#ed9998` `#e34948` `#a82523` |
| 다크 | `#5598e7` `#2a78d6` `#184f95` | `#383835` | `#8f2321` `#c33a38` `#e66767` |

- 타일마다 배경색과 잉크색을 짝지어 **글자 대비를 항상 확보**
- **비상장사는 색이 아니라 `비상장` 라벨**로 구분 (색 단독 의존 금지)
- 색에 의존하지 않는 **표 보기**를 함께 제공

### 데이터 소스 결정

- 주가·시가총액·52주 고저·차트 → **Yahoo Finance** (키 불필요, 15분 캐시)
- 회사별 뉴스 → **네이버 뉴스 검색 API** (기존 키 재사용)
- 재무·주주 → 처음엔 내가 넣은 개략치 → **3기에서 DART 공시값으로 전량 교체**

### 티커 실측 검증 (추정 금지)

상장 여부를 기억에 의존하지 않고 Yahoo 에 전부 조회해 확인했다. 그 결과 두 건이 뒤집혔다.

- **메리츠화재** — `000060.KS` 는 조회 실패. 2023년 메리츠금융지주와 포괄적 주식교환으로
  **상장폐지**된 것을 확인. 비상장으로 두고 지주(`138040.KS`) 시세를 참고 표시.
- **서울보증보험** — 추측했던 코드가 다른 회사(Macquarie Korea Infrastructure Fund)였다.
  Yahoo 검색으로 `031210.KS` 확정.

최종 상장 12개사: 삼성생명 `032830` · 한화생명 `088350` · 미래에셋생명 `085620` ·
동양생명 `082640` · 삼성화재 `000810` · DB손보 `005830` · 현대해상 `001450` ·
한화손보 `000370` · 롯데손보 `000400` · 흥국화재 `000540` · 코리안리 `003690` ·
서울보증보험 `031210`

### 회사별 뉴스 — 5차례 반복해서 고친 부분

가장 많이 틀렸고 가장 많이 고친 곳이다. 실제로 돌려보고 결과를 읽어야만 보이는 문제들이었다.

| 회차 | 증상 | 원인 | 대응 |
|------|------|------|------|
| 1 | 메리츠화재·교보생명에 무관한 브리핑 기사 | 회사명이 **본문에만** 스친 기사 채택 | 제목 매칭 우선, 묶음기사 후순위 (등급제) |
| 2 | **삼성생명 = 여자농구단 기사 3건**, 교보생명 = 재단·수영부 | 제목에 회사명이 있어도 업 소식이 아님 | 스포츠·재단 제외 + 보험업 관련성 게이트 |
| 3 | 코리안리 3건이 **같은 메가존클라우드 기사** | 문자열 유사도만으론 받아쓴 보도자료를 못 잡음 | 토큰 겹침 + 공유 토큰 수 + **같은 수치(182억)** + 첫 단어 |
| 4 | 메리츠화재에 **홈플러스 매각 기사** | 본문 언급 fallback 자체가 신뢰 불가 | **본문 fallback 폐기 — 제목에 없으면 안 씀** |
| 5 | 삼성생명에 자동생성 시세 기사 | "주가 265,500원 6.84% 하락 마감" | 시세봇 정규식 제외 (패널에 이미 차트가 있음) |

**판단: 3건을 억지로 채우기보다 없으면 없다고 한다.** 현재 37개사 중 뉴스 0건은
메리츠화재 1곳뿐이고, 나머지는 전부 해당 회사의 단독 업무 기사다.

추가로 언론 약칭 11건을 `aliases` 에 보강(카카오페이손보·농협손보·하나손보·캐롯손보 등).

### 트리맵에서 실제로 발견한 문제

**재보험·보증 그룹이 읽을 수 없는 띠로 눌렸다.** 업계 총자산의 2% 남짓이라 면적 비례로는
20px 높이가 된다. 면적을 부풀려 속이면 회사 타일의 전역 비교가 깨지므로,
**그 그룹만 균등폭 칩으로 바꾸고 헤더에 "좁아서 균등폭 표시"라고 밝혔다.**

### 다크모드에서 실제로 발견한 문제

첫 렌더에서 **+5.28%가 연한 분홍, +0.57%가 진한 빨강**으로 거꾸로 읽혔다.
다크 배경에서는 "큰 변동 = 더 밝고 진하게"가 맞는데, 밝은 끝에 저채도 색을 써서
큰 변동이 약하게 보였다. 램프를 다시 잡고 재검증했다.

### 결과물

- 히트맵: 3그룹 squarified treemap, 분야·크기기준·통화 필터, 표 보기
- 회사 패널: 현재가+등락 · 60거래일 차트(크로스헤어) · 재무 stat tiles · 주주 · 뉴스 3건
- `#c=<회사id>` 딥링크 — 주소 복사로 특정 회사 패널 공유
- 관리자모드: `/admin/news`(기존 기능 그대로) + `/admin/companies`(신규)

---

## 4. 3기 — DART 전자공시 연동

### 계기

2기 종료 시 재무·주주는 내가 채운 개략치였고, 그 사실을 화면과 보고에 명시했다.
사용자가 DART API 키를 제공해 **실제 공시값으로 교체**했다.

### 확인한 사실 (전부 실측)

- `corpCode.xml` — 118,562개사 인덱스 (ZIP → XML, 7일 캐시)
- `fnlttSinglAcnt.json` — **비상장사도 조회됨** (교보생명 등). 한 번 호출하면 별도·연결이 함께 온다
- `hyslrSttus.json` — 최대주주 및 특수관계인. 상장(`corp_cls=Y`)·비상장(`E`) 모두 동작
- `majorstock.json` — 5% 이상 대량보유 (국민연금·BlackRock 등, 상장사만)
- **2025 사업보고서까지 이미 제공** → 기준일 2025-12-31

### 별도 vs 연결

보험업계 규모 비교 관행이 별도 기준이라 **별도 우선, 없으면 연결**로 정하고
**어느 기준을 썼는지 `fs_basis` 에 기록해 패널에 표기**했다. 섞어 쓰면서 침묵하지 않는 것이 요점.
(삼성생명 별도 309.9조 / 연결 350.7조 — 통상 인용되는 수치는 별도에 가깝다.)

### 상호 매핑 — 자동 매칭을 폐기한 이유

DART 는 약칭이 아니라 등기 상호를 쓴다 (메리츠화재 → `메리츠화재해상보험`,
KB라이프 → `케이비라이프생명보험`, MG손보 → `엠지손해보험`).
유사도 자동 매칭을 시도했더니 **'삼성생명'이 지주회사 '삼성'과 1.0으로 매칭**됐다.
한화생명→한화, NH농협생명→NH농협손해보험, 흥국생명→흥국화재 등 다수가 오매칭.

→ `add_dart_codes.py` 에 **37건을 명시 매핑**하고, 저장 전에 등기 상호를 되짚어 검증.
37건 전부 일치 확인 후에만 파일을 쓴다.

### 내 개략치가 실제로 얼마나 틀렸는지

| 항목 | 내 추정 | DART 공시 |
|------|---------|-----------|
| 삼성생명 자본총계 | 38조 | **56.4조** |
| 신한라이프 자산총계 | 74조 | **59.4조** |
| NH농협생명 자산총계 | 72조 | **52.6조** |
| 푸본현대생명 순이익 | +500억 | **-1,187억 (적자)** |
| 흥국생명 순이익 | 900억 | **3,393억** |
| 롯데손해보험 순이익 | -200억 | **+513억** |

PER·PBR·ROE 도 함께 재계산됐다 (삼성생명 PER 25.0→27.9, PBR 1.25→0.84).
**개략치를 그대로 쓰면 안 되는 이유가 수치로 확인됐다.**

### DART 로 못 채운 13개사

메트라이프 · IBK연금 · iM라이프 · 라이나 · AIA · 하나생명 · 처브라이프 ·
교보라이프플래닛 · AXA손보 · MG손보 · 하나손보 · 캐롯 · 카카오페이손보

증권을 발행하지 않아 **사업보고서 제출 의무가 없고 감사보고서만** 낸다.
재무제표 API · 전체재무제표 API · 2023년까지 모두 확인했으나 데이터가 없다.

→ 억지로 채우지 않고 **개략치로 남기고 그렇게 표시**했다. 대신 37개사 전부에
**공시 원문 링크**를 붙여(정기보고서 또는 감사보고서) 열어 보고 직접 채울 수 있게 했다.

### K-ICS

DART 재무제표에 없는 감독지표다. 37개사 전부 **직접 입력값**으로 남기고
패널 stat tile 에 `직접 입력`이라고 표기했다.

### 3기에서 발견하고 고친 버그

1. **cp949 콘솔 크래시** — `—`(U+2014) 를 print 하면 한국어 윈도우 콘솔에서 죽는다.
   `add_dart_codes.py` 가 실제로 마지막 줄에서 죽었고, **기존 다이제스트 파이프라인
   (`src/collect.py`)에도 같은 문제가 잠재**해 있었다. 6곳을 찾아 고치고
   `src/console.py` 로 재발을 막았다.
2. **결산일 오인** — 손익계산서의 `2025.01.01 ~ 2025.12.31` 을 결산일로 읽어
   기준일이 `2025-01-01` 이 됐다. 재무상태표(`sj_div=BS`) 기준일만 쓰도록 수정.
3. **주주 중복 계상** — `삼성물산 43.56%`(특별관계자 합산)와 `삼성물산(주) 19.34%`(본인)가
   함께 표시됐다. `(주)` 표기 차이로 중복 제거가 실패한 것 → 이름 정규화로 해결.
4. **국민연금 누락** — 특수관계인 1.15%가 국민연금 6.87%를 밀어내고 목록 상한에 걸렸다.
   두 출처를 합쳐 지분율 순으로 정렬.
5. **낡은 테스트** — 데이터가 바뀌자 `verified is False` 를 하드코딩한 테스트가 깨졌다.
   원본 값을 붙잡아 비교하도록 수정 (코드가 아니라 테스트가 잘못이었다).

---

## 5. 4기 — 웹 배포 (Cloudflare Pages + Access)

### 요구사항

> 이 모든 기능을 웹에 업로드해서 어디서나 접속이 되게끔 하고싶어
> (뉴스 텔레그램 비중치 등 조절하는 기존기능도 웹에다가 그대로 이관) 무료로

### 확인한 선택

| 항목 | 선택 | 근거 |
|------|------|------|
| 공개 범위 | ~~Cloudflare Pages + Access~~ → **자체 비밀번호 게이트** | 아래 5.1 참고 — Access 는 쓸 수 없었다 |
| 데이터 방식 | **주기 스냅샷 (Actions cron)** | 콜드스타트 0초. 주가는 최대 30분 지연 |

### 무료 대안을 검토하고 탈락시킨 이유

| 방식 | 탈락 이유 |
|------|-----------|
| GitHub Pages | 공개 리포에선 비공개 불가 — 로그인을 걸 수 없다 |
| Netlify | 비밀번호 보호가 유료 |
| Render (Flask 그대로) | 15분 뒤 잠들어 첫 진입 30~60초. 파일시스템 휘발성 |
| PythonAnywhere | 외부 네트워크 화이트리스트 — Yahoo·DART 차단 |

"로그인해야 보기" 조건에서 완전 무료인 것은 Cloudflare Pages + Access 뿐이었다.

### 관리자 기능 이관 — 서버 없이

정적 호스팅에는 실행할 백엔드가 없다. 그런데 설정은 영구히 남아야 하고,
다이제스트는 이미 GitHub 의 `settings.json` 을 읽는다.
→ **브라우저가 GitHub Contents API 로 직접 커밋**한다.

```
static/js/admin.js          GitHub API 클라이언트 + 폼 렌더·직렬화
templates/admin_static.html 껍데기 (build_static.py 가 dist/admin/index.html 로 구움)
```

| 탭 | 대상 | 반영 |
|----|------|------|
| 뉴스·텔레그램 설정 | `settings.json` 커밋 | 익일 07시 다이제스트 |
| 보험사 데이터 | `data/insurers.json` 커밋 | 커밋 즉시 사이트 자동 재배포 |
| 배포·갱신 | `workflow_dispatch` × 3 + 최근 실행 현황 | 1~2분 |

토큰은 **브라우저 localStorage 에만** 저장되고 github.com 외로는 전송되지 않는다.
Fine-grained PAT, 이 리포만, Contents + Actions 쓰기 두 개만.
Cloudflare Access 로그인이 앞단에 있어 이중 방어가 된다.

### 워크플로우

| 파일 | 트리거 | 하는 일 |
|------|--------|---------|
| `site.yml` | 평일 09:00~16:30 30분마다 + 매일 07:20 + `data/`·`static/`·`templates/` 푸시 + 수동 | 스냅샷 빌드 → Cloudflare Pages 직접 업로드 |
| `sync-dart.yml` | 매월 5일 + 수동 | DART 갱신 → `data/insurers.json` 커밋 (→ site.yml 연쇄) |
| `daily.yml` | 매일 07:00 | 텔레그램 다이제스트 (**기존 그대로**) |

빌드 산출물을 git 에 커밋하지 않는다 — wrangler 가 러너에서 직접 업로드한다.
그래서 리포에 스냅샷 히스토리가 쌓이지 않는다.

### 4기에서 발견하고 고친 버그

1. **`[hidden]` 이 무시됨** — `.tabs { display: flex }` 가 브라우저 기본
   `[hidden] { display: none }` 을 덮어써서, **토큰 없이도 관리자 탭이 노출**됐다.
   헤드리스 브라우저로 확인 후 전역 규칙 `[hidden] { display: none !important }` 로 차단.
   (수정 후 `tabsHidden=true` 확인)
2. **CI 에서 시세 조회 재시도 없음** — 로컬은 15분 캐시가 막아주지만 Actions 는 매 실행이
   새 컨테이너다. 하루 16회 × 40여 건을 매번 새로 조회하므로 재시도가 없으면
   간헐적으로 "시세 조회 실패"가 섞여 배포된다. `market.py` 에 백오프 재시도 추가.

---

## 5.1. 배포 후 드러난 오판 — Access 를 쓸 수 없었다

4기 설계의 전제가 틀렸다. **Cloudflare Access 는 본인 계정에 등록된 도메인(zone)에만
걸린다.** `pages.dev` 는 Cloudflare 소유 공용 도메인이라 Access 대상이 되지 않고,
이 계정의 zone 은 0개였다. "무료로 로그인 걸기"의 근거였던 항목이 성립하지 않았다.

배포를 마치고 로그인을 걸려는 단계에서야 발견했다. Access 설정 화면에서 Domain 선택지에
`pages.dev` 가 나오지 않았고, API 로 계정을 조회해 zone 이 0개임을 확인했다.
**설계 시점에 조회 한 번이면 알 수 있었던 것을 문서만 보고 판단했다.**

### 선택지와 결정

| 방식 | 비용 | 인증 | 판단 |
|------|------|------|------|
| 도메인 구입 → Access | 연 1만원 안팎 | 이메일 OTP | 무료 조건이 깨짐 |
| **자체 비밀번호 게이트** | **무료** | 공유 비밀번호 | **채택** — 혼자 보는 용도에 충분 |
| 주소를 안 알리기 | 무료 | 없음 | 인증이 아니다 |

### 구현 — `gate/_worker.js`

Pages 는 배포 루트의 `_worker.js` 를 모든 요청의 앞단으로 쓴다. 이걸 이용해
정적 호스팅 위에서 인증을 한다. 서버를 새로 두지 않는다는 4기 원칙은 유지된다.

```
요청 → _worker.js → 쿠키 검사 → (유효) env.ASSETS 로 원본 전달
                              → (없음) 비밀번호 화면 401
```

- 비밀번호는 Pages 환경변수 `SITE_PASSWORD` (Secret). 코드·리포에 들어가지 않는다
- 쿠키는 `exp.HMAC-SHA256(SITE_PASSWORD, "v1|exp")` — 만료시각만 늘려도 서명이 깨진다
- 비교는 임의 키로 양쪽을 HMAC 해 고정 길이로 만든 뒤 수행 (길이·접두사 노출 방지)
- 비밀번호를 바꾸면 서명 키가 바뀌므로 기존 세션이 전부 무효가 된다
- **미설정이면 전부 차단(503).** 깜빡한 채 배포됐을 때 열리는 것보다 안 열리는 편이 낫다
- `site.yml` 이 `dist/_worker.js` 존재를 확인하고 없으면 배포를 중단한다

KV 없이 시도 횟수를 셀 수 없어 실패에 0.7초 지연만 뒀다. 근본 대비는 긴 비밀번호다.

### 검증

노드로 워커를 직접 호출해 25개 경로를 확인했다 (`25 PASS / 0 FAIL`).
미설정 차단 · 무쿠키 401 · 관리자 경로 차단 · 틀린 비밀번호 · **접두사만 같은 값** ·
**한 글자 짧은 값** · 서명 변조 · **만료시각만 늘린 쿠키** · 만료 쿠키 ·
비밀번호 변경 시 기존 세션 무효 · 쿠키 플래그(HttpOnly/Secure/SameSite) · 로그아웃.

각 차단 경로마다 **응답 본문에 실제 사이트 내용이 섞이지 않는지**까지 확인했다.
상태 코드만 401 이고 본문이 새면 막은 게 아니다.

### 배포하고 나서야 보인 것 — 게이트 이전 배포가 열려 있었다

게이트를 배포한 뒤 실제로 접속해 보니 정식 주소는 막혔는데
`ec8071ad.insuranceinfo.pages.dev` 가 메인·관리자 모두 200 으로 열렸다.

Pages 는 **배포마다 고유 주소를 발급하고 그 주소는 계속 살아 있다.** 게이트는 코드에
들어 있으므로, 그 코드가 없던 시절의 배포는 새 배포를 아무리 해도 막히지 않는다.
지워야 닫힌다. 게이트 이전 배포 2건을 삭제하고 404 를 확인했다.

앞으로의 배포는 전부 게이트를 포함하므로 이 문제는 재발하지 않는다. 다만
**게이트를 잠시라도 뺀 채 배포하면 그 배포 주소가 영구히 열린다.** `site.yml` 이
`dist/_worker.js` 존재를 확인하고 없으면 배포를 중단하는 이유가 이것이다.

교훈은 5기에서도 같다 — **켰다고 끝이 아니라, 켠 뒤에 실제로 막혔는지 확인해야 한다.**
정식 주소만 보고 끝냈으면 관리자 화면이 열린 주소를 하나 남긴 채 마무리했을 것이다.

---

## 6. 5기 — FISIS 감독통계 연동 (K-ICS + 증권 미발행사 재무)

### 남아 있던 두 구멍

3기가 끝나고도 두 가지가 개략치로 남아 있었다.

- **K-ICS 37개사 전부** — DART 재무제표에 없는 감독지표라 손으로 넣은 값이었다
- **증권 미발행 13개사 재무** — 사업보고서 제출 의무가 없어 DART 에 데이터가 없다

둘 다 `350.0` `200.0` `240000` `90000` 같은 반올림 숫자였다. 실측이 아니었다.

### FISIS 를 고른 이유 — 확인하고 나서 정했다

[FISIS(금융감독원 금융통계정보시스템)](https://fisis.fss.or.kr) 은 보험사가 **감독당국에
매분기 내는 업무보고서**에서 뽑은 통계를 무료 API 로 준다. DART 가 '증권을 발행한 회사'만
담는 것과 달리, 업무보고서는 **모든 보험사가 낸다.** 그래서 두 구멍이 한 출처로 메워진다.

제안 당시에는 K-ICS 가 실제로 있는지 확인하지 못한 상태였고, 그 사실을 밝히고 시작했다.
Access 건에서 확인 안 한 전제로 설계했다가 배포 직전에 뒤집힌 직후였다. 키를 받고
통계표 목록부터 조회했다.

| 통계표 | 내용 |
|--------|------|
| `SH021` / `SI021` 자본적정성 | **지급여력비율** — 경과조치 적용 전·후 모두 |
| `SH150` / `SI146` | 자산총계 |
| `SH151` / `SI147` | 부채총계 · 자본총계 · 보험계약부채 |
| `SH154` / `SI150` | 영업이익 · 당기순이익 |

권역은 `partDiv` 로 나뉜다 — `H`=생명보험, `I`=손해보험.
코리안리·서울보증은 우리 화면에서 '재보험·보증'이지만 FISIS 에서는 손해보험이다.

### 실측으로 걸러낸 것 세 가지

**① 손익은 분기 단독값이다.** 처음엔 202512 한 번만 읽어 연간으로 썼다. 삼성화재
당기순이익이 DART 16,909억인데 FISIS 는 2,277억으로 나왔다. 4분기 3개월치였다.
네 분기를 더하니 `5,556+3,982+5,093+2,277 = 16,908억` — DART 와 반올림 1억 차이로 맞았다.
`term=Y` 로 바꿔도 같은 값이 온다(주기 파라미터일 뿐이다). **한 분기라도 비면 합계를
쓰지 않고 `None` 으로 둔다.** 반쪽 숫자를 연간값처럼 보이게 하지 않기 위해서다.

**② 자산총계는 DART 와 원 단위까지 같고, 부채·자본 배분은 다르다.**
삼성생명 자산총계는 두 출처 모두 3,099,483억으로 완전히 같은데, 자본총계는
DART 56.4조 / FISIS 43.6조다(자산 = 부채 + 자본은 양쪽 다 성립한다).
감독업무보고서와 IFRS 별도재무제표의 평가 기준이 다르기 때문이다.

→ **DART 가 있으면 DART 로 통일하고, 없는 회사만 FISIS 로 채운다.** 같은 화면에서
두 기준을 섞으면 회사 간 비교가 깨진다. 대신 `fs_basis` 에 어느 쪽인지 남기고
패널에 "업무보고서 · 감독회계 (FISIS)" 라고 표기한다.
당기순이익은 검증한 3개사 모두 **0.00% 오차로 일치**했다.

**③ 경과조치를 신청한 회사와 아닌 회사가 섞여 있다.** 신청하지 않은 회사는
'적용 후' 값이 `0` 으로 온다. 그대로 쓰면 삼성생명 K-ICS 가 0% 가 된다.
→ **적용 후가 0보다 크면 그 값, 아니면 적용 전**을 쓰고 `kics_basis` 에 어느 쪽인지 남긴다.
35개사 중 28개사가 적용 후 기준이다. 금감원 보도자료의 업계 수치도 적용 후 기준이다.

### 상호 매핑 — 또 자동 매칭을 폐기했다

3기와 같은 문제가 그대로 재현됐다. 이름 유사도로 맞추니 **'하나생명보험'과
'하나손해보험'이 서로 붙었고**, 캐롯은 폐업 상태라 아예 못 찾았다.
→ `add_fisis_codes.py` 에 **35건 명시 매핑**, 저장 전 FISIS 상호·권역을 되짚어 전건 확인.

### 매핑하다 발견한 것 — 없는 회사 2곳을 그리고 있었다

기준일이 2025-12-31 인데, 그 시점에 존재하지 않는 회사가 목록에 있었다.

- **MG손해보험** — 2025-09-03 금융위가 영업정지를 의결하고 보험계약 122만건을
  가교보험사 **예별손해보험**(예금보험공사 100% 출자)으로 이전했다. 09-04 영업 전면 정지.
  FISIS 보고도 2025.06 이 마지막이고 K-ICS 는 `64.02 → 42.71 → 3.45 → -19.34` 로 무너졌다.
  우리 데이터의 `50.0` 은 어느 시점과도 맞지 않았다.
- **캐롯손해보험** — 모회사 한화손해보험에 흡수합병. FISIS 에 `[폐]` 로 남아 있고
  2025.09 가 마지막 보고다. 실적은 한화손해보험 수치에 이미 포함된다.

→ 둘을 제거해 **37개사 → 35개사**. 없는 회사를 그리면 업계 지형을 잘못 읽게 된다.
제거 사유는 `add_fisis_codes.py` 의 `REMOVED` 에 남겼다.

### 개략치가 실제로 얼마나 틀렸는지 (또다시)

| 항목 | 개략치 | FISIS 실측 |
|------|--------|-----------|
| AIA생명 자산총계 | 7.0조 | **19.5조** |
| AXA손해보험 자산총계 | 5.0조 | **0.9조** |
| 라이나생명 자본총계 | 2.0조 | **6.0조** |
| 카카오페이손보 자산총계 | 5,000억 | **1,591억** |
| NH농협생명 K-ICS | 200.0 | **412.99** |
| AIA생명 K-ICS | 350.0 | **204.6** |
| 메트라이프생명 K-ICS | 350.0 | **269.59** |

3기에서 "개략치를 그대로 쓰면 안 된다"고 적었는데, 남겨둔 개략치가 다시 같은 크기로 틀렸다.
삼성생명 K-ICS `193.0` 도 실제 `197.97` 이었고, 이 값은 언론 보도치 198.0% 와 일치한다.

### 결과물

- `src/fisis.py` — FISIS 클라이언트 (계정을 **코드가 아니라 이름으로** 고른다. 서식이
  개정되면 코드는 밀리지만 이름은 남는다)
- `add_fisis_codes.py` — 35건 명시 매핑 + 소멸 2개사 제거 (1회성)
- `sync_fisis.py` — K-ICS 전 회사 + 증권 미발행사 재무 갱신
- `.github/workflows/sync-fisis.yml` — 매월 5일 11시 (sync-dart 다음)
- 관리자 화면에 **[K-ICS·감독통계 갱신]** 버튼

`verified` 만 보고 건너뛰면 FISIS 로 채운 회사가 첫 실행 뒤 영영 갱신되지 않는다.
`fs_basis` 가 FISIS 인 회사는 계속 FISIS 로 갱신하도록 조건을 나눴다.

### 워크플로우를 실제로 돌려서 잡은 것

로컬에서만 돌려봤을 뿐 `sync-dart.yml`·`sync-fisis.yml` 은 한 번도 실행된 적이 없었다.
둘 다 돌려보니 두 가지가 나왔다.

**① `DART_API_KEY` 가 등록돼 있지 않았다.** 지금까지 DART 값은 전부 로컬에서 조회해
커밋한 것이라 워크플로우 경로를 한 번도 밟지 않았고, 그래서 아무도 몰랐다.
매월 5일에 조용히 실패하고 있었을 것이다.

**② 두 sync 가 `data_note` 를 서로 지웠다.** `sync_dart.py` 가 자기 문구를 통째로
덮어쓰는데, 그 문구는 FISIS 를 붙이기 전에 쓴 것이라 "K-ICS 는 관리자모드에서 직접
관리합니다" 였다. sync-dart 를 돌리자 **데이터는 멀쩡한데 설명만 거짓이 됐다.**
(회사별 재무·K-ICS 는 전혀 건드려지지 않았다 — 1줄만 바뀌었다.)

→ 문구를 `settings_store.DATA_NOTE` 한 곳으로 모으고 두 스크립트가 그것을 쓰게 했다.
같은 파일을 여러 갱신 경로가 쓰면, **각자 쓰는 필드는 반드시 갈라놓아야 한다.**
이건 로컬 단위 실행으로는 절대 안 보이고 두 워크플로우를 순서대로 돌려야만 보인다.

---

## 6.1. 6기 — 이름·서체·화면 개선

### 요구사항

> 사이트 제목을 Genie's Insurance Note로 바꿔주고 / KoPub_돋움체로 바꿔줘 /
> 전반적으로 ux/ui를 개선해줘 (디자인 페이지 구성도) 이건 너가 추천한 방안으로해줘

### 서체 — CDN 을 쓰지 않은 이유

KoPubWorld 돋움은 jsDelivr 에 있지만(`font-kopubworld`) 가중치당 1.9MB 이고
무엇보다 **외부 CDN 이 막히면 글꼴만 조용히 깨집니다.** 사내망에서 쓸 화면이라
그 위험을 감수할 이유가 없었습니다.

→ `pyftsubset` 으로 서브셋해 **리포에 넣고 같은 출처에서 서비스**합니다.

| | 원본 | 서브셋 |
|---|---|---|
| Light | 1,864KB | **286KB** |
| Medium | 1,982KB | **323KB** |
| Bold | 2,008KB | **305KB** |

원본이 큰 건 일본어·한자 글리프 때문이고, 한글 음절은 **11,172자 전부 남겼습니다**
(자주 쓰는 2,350자만 남기면 드문 음절에서 시스템 폰트로 튀어 한 줄 안에서 서체가 섞입니다).
이모지는 서브셋에 없어 시스템 이모지 폰트로 넘어갑니다 — 의도한 동작입니다.

경로는 로컬 Flask 와 정적 스냅샷이 **`/fonts/...` 하나로 같습니다.**
스냅샷은 CSS 가 HTML 에 인라인돼 상대경로가 루트 기준이 되는데, Flask 는 CSS 가
`/static/css/` 에 있어 어긋납니다. Flask 쪽에 `/fonts/` 라우트를 두어 맞췄습니다.

KoPub 돋움은 굵기가 **300/400/700 세 단계뿐**이라 500·600 은 쓰지 않습니다
(브라우저가 합성하거나 400 으로 떨어뜨려 위계가 무너집니다).
시스템 산세리프보다 x-height 가 작아 본문을 14px → **15px** 로 올렸습니다.

### 화면 개선 — 무엇을 고쳤나

**① 요약 지표 줄을 새로 넣었다.** 히트맵은 개별 회사를 보여주지만 '업계가 지금 어떤
상태인가'는 못 보여줍니다. 총자산·회사 수·오늘 등락·K-ICS 중앙값·기준일을 상단에 뒀습니다.
K-ICS 는 평균이 아니라 **중앙값**입니다 — 규모 차이가 커서 단순평균은 소형사 한 곳
(400%대)에 끌려가고, 가중평균은 '무엇으로 가중했는지'를 또 설명해야 합니다.

**② 회사 검색을 넣었다.** 35개사를 트리맵에서 눈으로 찾는 건 느립니다.
`/` 로 어디서나 포커스, Enter 로 첫 결과 열기. 검색어에 안 걸린 타일은 **지우지 않고
흐리게** 합니다 — 지우면 면적 비례가 깨져 '전체 중 어디쯤'이라는 정보를 잃습니다.

**③ 타일 이름이 조각나던 것을 고쳤다.** 이전에는 `MG손해보 / 험`, `iM라이프생 / 명`
처럼 끊겼습니다. 글자 폭을 **한글 1em · 라틴 0.55em** 으로 계산해 한 줄에 맞는 크기까지
줄이고, 그래도 안 들어가면 이름을 아예 빼고 툴팁·표에 맡깁니다.
**줄바꿈은 아예 금지했습니다** — 쪼개진 이름보다 이름이 없는 편이 낫습니다.

**④ 표 보기에 정렬과 K-ICS 열을 넣었다.** 머리글 클릭으로 정렬하고, 빈 값은 방향과
무관하게 항상 아래로 보냅니다(빈 값이 위에 오면 정렬이 고장난 것처럼 보입니다).

**⑤ 각주를 표처럼 바꿨다.** 출처 표기는 이 프로젝트의 원칙이라 숨기지 않되,
통짜 문단이면 아무도 안 읽습니다. 항목별로 끊고 `실측`·`공시`·`감독`·`추정` 라벨을 세웠습니다.

**⑥ 패널 머리글을 고정했다.** 재무·주주·뉴스를 내려보는 동안 회사 이름이 남습니다.

### 스크린샷 도구에 속을 뻔한 것

모바일 확인에서 트리맵이 오른쪽으로 잘려 보여 가로 오버플로로 판단했습니다.
그런데 브라우저에서 직접 재보니 **`--window-size=430` 을 줘도 실제 뷰포트는 489px**
이었습니다. 잘린 건 레이아웃이 아니라 스크린샷 프레임이었습니다.

이 환경의 헤드리스 크롬은 창을 489px 아래로 줄이지 않아, 진짜 모바일 폭은
**iframe 을 390px 로 만들어** 그 안에서 측정했습니다 — `vw=375, scrollW=375`,
오버플로 없음. 489·667·967·1567 에서도 전부 없음.

**보이는 것만으로 원인을 정하면 안 된다**는 사례가 하나 더 늘었습니다.
고칠 필요가 없는 것을 고칠 뻔했습니다.

### 게이트에 낸 구멍과 그 검증

로그인 화면도 같은 서체로 보이려면 `/fonts/` 가 인증 전에 나가야 합니다.
KoPub 은 누구나 받을 수 있는 공개 배포 글꼴이라 새는 정보는 없지만,
**구멍을 냈으면 그 크기를 재야 합니다.** 테스트를 25건 → **31건**으로 늘렸습니다.

추가한 6건: 폰트 무인증 통과 · `/fonts/` 아래 non-woff2 차단 · 디렉터리 자체 차단 ·
쿼리로 위장한 `.woff2` 차단 · 유사 경로(`/fontsX/`) 차단 · 상위경로 탈출 차단.
차단 경로마다 **응답 본문에 사이트 내용이 섞이지 않는지**까지 확인합니다.

---

## 7. 데이터 출처와 신뢰도 (현재)

| 항목 | 출처 | 신뢰도 | 갱신 |
|------|------|--------|------|
| 주가 · 전일대비 · 시가총액 · 52주 고저 · 차트 | Yahoo Finance | 실측 | 30분 주기 |
| 자산·부채·자본·영업이익·순이익·보험계약부채 | DART 사업보고서 | **공시값 (24개사)** | 월 1회 + 수동 |
| 〃 (증권 미발행사) | FISIS 업무보고서 | **감독제출값 (11개사)** | 월 1회 + 수동 |
| 주요 주주 | DART 최대주주 현황 + 5% 대량보유 | **공시값 (24개사)** | 월 1회 + 수동 |
| 최근 주요 뉴스 | 네이버 뉴스 검색 | 실측 | 30분 주기 |
| PER · PBR · ROE | 시총 ÷ 위 재무값 | **추정치** | 위와 동일 |
| **K-ICS 비율** | **FISIS 업무보고서** | **감독제출값 (35개사)** | 월 1회 + 수동 |

**현재 데이터**: 35개사(생보 21 · 손보 12 · 재보험·보증 2), 상장 12 / 비상장 23,
**개략치 0** — 재무는 DART 24 + FISIS 11, K-ICS 는 35개사 전부 FISIS 실측.
기준일 2025-12-31, 업계 총자산 합계 1,334.2조, 주주·공시링크 전 회사 보유.

재무 기준이 두 종류(IFRS 별도 / 감독회계)라 **회사별로 어느 쪽인지 패널에 표기**한다.
K-ICS 도 경과조치 적용 전·후가 섞여 있어 **회사마다 어느 기준인지 함께 표기**한다.

---

## 8. 검증 방법과 결과

원칙: **기억이나 눈대중으로 판단하지 않고 실제로 돌려서 확인한다.**

| 대상 | 방법 | 결과 |
|------|------|------|
| 티커 12건 | Yahoo 실제 조회 | 전부 확인, 2건 정정(메리츠화재 상장폐지·서울보증 코드) |
| DART 상호 37건 | corpCode 역조회 검증 | 전부 일치 (자동 유사도 매칭은 폐기) |
| FISIS 상호·권역 35건 | companySearch 역조회 검증 | 전부 일치, 소멸 2개사 발견해 제거 |
| FISIS 재무 | 같은 회사를 DART 와 대조 | 자산총계 원 단위 일치 · **당기순이익 0.00% 오차** |
| FISIS 손익 기간 | 4개 분기 합 vs DART 연간 | 16,908 vs 16,909 (분기 단독값임을 확인) |
| 비밀번호 게이트 | 노드로 워커 직접 호출 | **31/31 PASS** (폰트 예외 6건 포함, 본문 유출까지 확인) |
| 가로 오버플로 | iframe 390px + 실제 뷰포트 4종 측정 | 전 폭에서 없음 (창 크기 ≠ 뷰포트임을 확인) |
| 폰트 서브셋 | cmap 커버리지 대조 | 한글 11,172/11,172 · 데이터 문자 누락 0 (이모지 제외) |
| 배포 차단 | 정식·배포별 주소 전수 접속 | 구배포 1건이 열려 있어 삭제 |
| 색 팔레트 | 검증 스크립트 (명도 단조·ΔL·대비) | 라이트·다크 양쪽 PASS |
| 화면 | 헤드리스 Chrome 스크린샷 | 히트맵·패널·표·다크·관리자 전부 육안 확인 |
| 뉴스 품질 | 회사별 결과를 UTF-8 파일로 덤프해 직접 읽기 | 5회 반복 교정 |
| 관리자 로직 | 폼 파싱·저장 왕복·파생지표 경계값 | **18/18 PASS** |
| 라우트 | 로컬 HTTP 실제 요청 | 200 × 4, 404 정상 |
| 관리자 인증 | 브라우저에서 무토큰·오류토큰 재현 | 탭 숨김·401 안내 정상 |
| 키 유출 | `dist/` 전수 검사 | 없음 |
| 워크플로우 | YAML 파싱 + JS `node --check` | 전부 OK |

관리자 저장 테스트는 **git push 를 유발하지 않는 방식**으로 했다 (라우트 대신 파싱·저장
함수를 직접 호출). 작업 중 임의 커밋·푸시는 하지 않았다.

---

## 9. 파일 지도

**정보사이트**
| 파일 | 줄수 | 역할 |
|------|------|------|
| `webapp.py` | 328 | Flask 라우트 (사이트 + 관리자 + API) |
| `data/insurers.json` | 1760 | 보험사 37개사 마스터 |
| `static/js/app.js` | 768 | treemap · 패널 · SVG 차트 · 표 보기 |
| `static/css/app.css` | 1177 | 팔레트 · 레이아웃 · 라이트/다크 |
| `templates/_site.html` | 97 | 히트맵 본문 (로컬·스냅샷 공용) |

**데이터 수집**
| 파일 | 줄수 | 역할 |
|------|------|------|
| `src/dart.py` | 405 | DART 재무·주주·공시원문 (비상장 포함) |
| `src/fisis.py` | 212 | FISIS 감독통계 — K-ICS·증권 미발행사 재무 |
| `src/market.py` | 207 | Yahoo 시세·시총·환율 + PER·PBR·ROE |
| `src/company_news.py` | 360 | 회사별 뉴스 (제목매칭·관련성·중복제거) |
| `sync_dart.py` | 167 | DART → insurers.json 갱신 |
| `sync_fisis.py` | 184 | FISIS → K-ICS 전 회사 + 미발행사 재무 |
| `add_fisis_codes.py` | 178 | FISIS 상호 검증 + 소멸사 제거 (1회성) |
| `add_dart_codes.py` | 121 | 상호 검증 후 고유번호 부여 (1회성) |

**웹 배포**
| 파일 | 줄수 | 역할 |
|------|------|------|
| `build_static.py` | 246 | `dist/index.html` + `dist/admin/index.html` |
| `static/js/admin.js` | 558 | 웹 관리자 — GitHub API 클라이언트 |
| `templates/admin_static.html` | 119 | 웹 관리자 껍데기 |
| `.github/workflows/site.yml` | 68 | 스냅샷 → Cloudflare Pages |
| `.github/workflows/sync-dart.yml` | 60 | DART 갱신 → 커밋 |
| `.github/workflows/sync-fisis.yml` | 63 | K-ICS·감독통계 갱신 → 커밋 |
| `gate/_worker.js` | 190 | 비밀번호 게이트 (Pages 앞단) |

**보조** — `src/settings_store.py`(179) · `src/console.py`(21) · `dashboard.py`(21, 구 진입점 shim) ·
`.bat` 4개(정보사이트 실행 · 스냅샷 빌드 · DART갱신 · 구 대시보드)

**변경 없음** — `main.py` · `config.py` · `src/{collect,filters,classify,rank,summarize,notify}.py` ·
`daily.yml` (print 문의 cp949 문자만 교체)

---

## 10. 남은 일

### 사용자 작업 (배포)

푸시 전에 ②③④를 먼저 (Secrets 없이 푸시하면 첫 Actions 실행이 실패한다).

- [x] Cloudflare 가입 → Pages 프로젝트 **`insuranceinfo`** 생성 (하이픈 없는 이름으로 생성됨 → site.yml 반영)
- [x] API 토큰(Cloudflare Pages · Edit) + Account ID 확보
- [x] GitHub Secrets 등록 (`site.yml` 은 네이버 키도 쓴다 — 총 5개)
- [x] 커밋·푸시 (35개 파일, 2·3·4기 3개 커밋)
- [x] 첫 배포 성공 — `https://insuranceinfo.pages.dev`, 전건 수집, 시세 실패 0
- [x] `SITE_PASSWORD` 등록 (Pages → Settings → Variables and Secrets, Secret 타입)
- [ ] **Actions 재실행** — Pages 환경변수는 다음 배포부터 적용된다.
      Direct Upload 프로젝트라 Cloudflare 쪽 retry 가 막혀 있어 워크플로우를 다시 돌려야 한다
- [x] `FISIS_API_KEY` · `DART_API_KEY` 등록 후 두 워크플로우 실제 실행 확인 (6장 말미)
- [ ] `/admin/` 에서 GitHub PAT 1회 입력
- [x] ~~DART 키 · Cloudflare 토큰 재발급~~ — **하지 않기로 결정.** 사이트 동작에는
      지장이 없고, 노출 범위가 이 대화에 한정된다는 판단

### 데이터 교정

- [x] 개략치 재무 — FISIS 업무보고서로 11개사 전량 교체 (6장 참고)
- [x] K-ICS — 35개사 전부 FISIS 실측값으로 교체, 경과조치 기준 표기
- [x] 소멸 2개사(MG손보·캐롯손보) 제거 → 35개사
- [x] 사이트 이름을 **Genie's Insurance Note** 로 변경, 본문 서체 KoPubWorld 돋움
- [x] UX/UI 개선 — 요약 지표 · 회사 검색 · 표 정렬 · 타일 이름 조각남 수정 (6.1 참고)
- [ ] 예별손해보험(가교보험사)을 목록에 넣을지 검토 — 122만 계약이 살아 있지만
      일반 보험사가 아니라 정리 목적의 한시 법인이다
- [ ] 신한EZ손해보험이 FISIS 에 있는데 우리 목록에 없다 — 누락인지 의도인지 확인

### 알려진 한계

- 같은 보도자료를 받아쓴 기사를 4개 신호로 걸러내지만 완벽하지 않다
- 제목에 회사명이 없는 기사는 쓰지 않으므로 단독 보도가 없는 날은 3건보다 적을 수 있다
- 시가총액 기준 히트맵은 상장사만 담는다 (비상장 추정 지분가치를 만들지 않는다)
- 대량보유 지분율은 특별관계자 합산이다. 이름 정규화로 중복을 막지만
  표기가 크게 다르면 같은 주주가 두 줄로 남을 수 있다
- 주가는 스냅샷 방식상 최대 30분 지연된다
- Actions 예약 실행은 GitHub 부하에 따라 지연·생략될 수 있다
- 비밀번호 게이트는 **공유 비밀번호 하나**다. 누가 열었는지 알 수 없고 개별 회수도 안 된다
  (전원 무효화만 가능). 사람별 인증·감사 로그가 필요하면 도메인을 사서 Access 로 가야 한다
- 재무 기준이 두 종류다. DART 24개사는 IFRS 별도, FISIS 11개사는 감독회계라
  **부채·자본 배분 방식이 다르다.** 자산총계 비교는 유효하지만 자본총계 비교는 주의해야 한다
- K-ICS 는 경과조치 적용 전·후가 섞여 있다(35개사 중 28개사가 적용 후). 회사 간 직접 비교 시
  기준이 다를 수 있으며, 패널에 어느 쪽인지 표기해 두었다
- FISIS 업무보고서는 분기 종료 후 공시까지 시차가 있다. 최신 분기가 아직 없으면
  기준월을 앞당겨 조회해야 한다
- 본문 서체 서브셋에 한자가 없다. 뉴스 제목에 한자가 섞이면 그 글자만 시스템 폰트로
  빠진다 (한자를 넣으면 서브셋이 다시 MB 단위가 된다)
- 회사 검색은 이름·영문명·티커만 본다. '생명'으로 검색하면 이름에 '생명'이 없는
  신한라이프·KDB생명 같은 회사는 걸리지 않는다

### 검토해볼 것

- 재보험·보증을 손해보험에 합치면 균등폭 칩 없이 정상 트리맵이 된다 (분야 구분 포기)
- 분기보고서(11012~11014)까지 받으면 기준일이 최대 3개월 앞당겨진다
- 손해율·CSM·수입보험료는 DART 주요계정에 없다 (전체재무제표 파싱 또는 수동)

---

## 11. 판단 원칙 (되짚어볼 것)

이 프로젝트에서 반복적으로 유효했던 판단들.

1. **추정값을 실측값처럼 보이게 하지 않는다.** 개략치는 화면·보고·배지에 개략치라고
   적었고, DART 로 교체되자 내 추정이 크게 틀렸음이 드러났다.
2. **없으면 없다고 한다.** 뉴스 3건을 억지로 채우면 홈플러스 기사가 올라온다.
   13개사 재무를 억지로 채우면 틀린 숫자가 공시값처럼 보인다.
3. **면적·색은 데이터다.** 재보험 그룹이 작으면 작게 그리고, 읽기 문제는 표현 방식으로
   푼다(균등폭 칩 + 명시). 면적을 부풀려 해결하지 않는다.
4. **기억으로 판정하지 않는다.** 티커·상호·색 대비 전부 실제로 조회하거나 계산했다.
   유사도 자동 매칭은 '삼성생명 = 삼성' 을 만들었고, FISIS 에서는 '하나생명 = 하나손보' 를
   만들었다. 같은 실수가 출처만 바뀌어 그대로 재현됐다 — 두 번 다 명시 매핑으로 갔다.
5. **돌려보고 결과를 읽는다.** 뉴스 품질 문제 5개, 다크모드 반전, 트리맵 압착,
   `[hidden]` 무시, cp949 크래시, FISIS 손익이 분기 단독값인 것 — 전부 실행 후에야 보였다.
6. **기준을 섞으면 밝힌다.** 별도/연결, DART/FISIS, 경과조치 적용 전/후,
   공시값/추정치를 항목마다 표기했다.
7. **설계의 전제도 실측 대상이다.** 4기에서 "Cloudflare Access 로 무료 로그인"을
   문서만 보고 정했다가, 배포를 마치고 로그인을 걸려는 단계에서 **계정에 도메인이 없으면
   Access 자체를 쓸 수 없다**는 걸 알았다. 조회 한 번이면 알 수 있는 것이었다.
   5기에서는 K-ICS 가 FISIS 에 있는지 **모른다고 먼저 밝히고**, 키를 받아 통계표를
   조회한 뒤에 설계했다.
8. **막았다고 끝이 아니라 막혔는지 확인한다.** 게이트를 배포하고 정식 주소만 보고
   끝냈으면, 관리자 화면이 인증 없이 열리는 구배포 주소를 남긴 채 마무리했을 것이다.
9. **없어진 것도 데이터다.** 소멸한 회사 2곳을 그대로 그리고 있었다. 새 출처를 붙이는
   김에 목록 자체가 현재와 맞는지 확인해야 한다.
